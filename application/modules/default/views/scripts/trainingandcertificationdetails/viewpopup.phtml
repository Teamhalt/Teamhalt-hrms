<?php 

?>

<?php $baseUrl = $this->serverUrl().$this->baseUrl();

?>
<div id="error_message" style="display:none;"></div>
<div class="poc-ui-data-control">
<?php echo $this->form->id; ?>
<?php
$issueddate = $this->form->issued_date->getValue(); 
sapp_Helper::viewHeader();
sapp_Helper::viewBody('alt','Course Name',(!empty($this->data[0]['course_name']))?$this->data[0]['course_name']:"...",'Course Level',(!empty($this->data[0]['course_level']))?$this->data[0]['course_level']:"...");
sapp_Helper::viewBody('nor','Course Offered By',(!empty($this->data[0]['course_offered_by']))?$this->data[0]['course_offered_by']:"...",'Description',(!empty($this->data[0]['description']))?$this->data[0]['description']:"...");
sapp_Helper::viewBody('alt','Certification Name',(!empty($this->data[0]['certification_name']))?$this->data[0]['certification_name']:"...",'Issued Date',(!empty($issueddate))?$this->form->issued_date->getValue():"...");
sapp_Helper::viewFooter();
?>
        <div class="new-form-ui-submit" id="formSubmit">
        <input onclick="closeiframepopup('trainingandcertificationdetails','cancel');" type="button" value="close"  />
        </div>
  		</div>      
</form>

<script type='text/javascript'>
$(document).ready(function(){
	//To get the Calender image...
	var yearrange = <?php echo "'".date("Y", strtotime("-30 years")).':'.date('Y')."'"; ?>;

	$('#from_date,#to_date').datepicker({
					showOn:'both',
					maxDate: new Date, 
					yearRange: yearrange,
					dateFormat:"<?php echo DATEFORMAT_JS;?>",
					buttonImage: "<?php echo $this->baseUrl("public/media/images/cal.jpg");?>",
					buttonImageOnly: true,
					buttonText: "",
					changeMonth: true,
                                        showButtonPanel: true ,
					changeYear: true 
	});
	
	 });
	 $("#certification_name").blur(function()
	 {
		if( $("#certification_name").val() != "")
		{
			$("#issuedDate").addClass("required");
		}
		else
		{
			$("#issuedDate").removeClass("required");
		}
	 });
</script>